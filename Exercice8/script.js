//Votre code ici
function add(a, b) {
    const one = parseFloat(a);
    const two = parseFloat(b);
    const c = parseFloat(one + two);
    return c;
}
const result = add(3, 8);
console.log(result);
 export default add;
